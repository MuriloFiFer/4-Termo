name: controle_acesso
description: "A new Flutter project."
publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

  # Dependências adicionais com a palavra-chave 'any'
  firebase_core: any
  firebase_auth: any
  cloud_firestore: any
  qr_code_scanner: any
  geolocator: any
  google_maps_flutter: any
  firebase_messaging: any

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
